import React from 'react';

const ExampleError = ({answer}) => {
    return (
        <React.Fragment>
            <span className="example-error">Не верно!</span>
        </React.Fragment>
    )
}

export default ExampleError;